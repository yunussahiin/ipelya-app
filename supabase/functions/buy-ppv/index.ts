// Bu dosya buy-ppv edge function kodu için boş şablondur.
export const handler = async (req: Request) => {
  // TODO: burada edge function iş mantığını uygula.
  return new Response(JSON.stringify({ message: "buy-ppv todo" }), { status: 200 });
};
