// Bu dosya enable-shadow-mode edge function kodu için boş şablondur.
export const handler = async (req: Request) => {
  // TODO: burada edge function iş mantığını uygula.
  return new Response(JSON.stringify({ message: "enable-shadow-mode todo" }), { status: 200 });
};
