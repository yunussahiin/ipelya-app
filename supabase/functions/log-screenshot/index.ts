// Bu dosya log-screenshot edge function kodu için boş şablondur.
export const handler = async (req: Request) => {
  // TODO: burada edge function iş mantığını uygula.
  return new Response(JSON.stringify({ message: "log-screenshot todo" }), { status: 200 });
};
