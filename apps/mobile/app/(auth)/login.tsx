import { PlaceholderScreen } from "@/components/layout/PlaceholderScreen";

export default function LoginScreen() {
  return (
    <PlaceholderScreen
      title="Giriş"
      description="Supabase auth + Secure Store ile oturum açma ekranı burada olacak."
    />
  );
}
