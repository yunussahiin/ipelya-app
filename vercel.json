{
  "buildCommand": "cd apps/web && pnpm run build",
  "installCommand": "corepack enable && corepack prepare pnpm@8.15.8 --activate && pnpm install --no-frozen-lockfile",
  "framework": "nextjs",
  "outputDirectory": "apps/web/.next"
}
