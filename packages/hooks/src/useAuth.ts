// Bu dosya mobil ve web tarafında kullanılacak ortak auth hook'unu özetler.
export function useAuth() {
  // TODO: Supabase session bilgisini React Query + Zustand ile bağla.
  return { session: null };
}
